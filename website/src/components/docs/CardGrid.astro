---
interface Props {
  cols?: 1 | 2 | 3;
}

const { cols = 2 } = Astro.props;

const gridClasses = {
  1: "grid-cols-1",
  2: "grid-cols-1 md:grid-cols-2",
  3: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3",
};
---

<div class={`grid ${gridClasses[cols]} gap-4 my-6 not-prose`}>
  <slot />
</div>
