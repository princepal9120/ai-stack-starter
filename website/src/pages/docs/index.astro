---
import DocsLayout from "../../layouts/DocsLayout.astro";
import Callout from "../../components/docs/Callout.astro";
import CodeBlock from "../../components/docs/CodeBlock.astro";
import CardGrid from "../../components/docs/CardGrid.astro";
import Card from "../../components/docs/Card.astro";
---

<DocsLayout title="Introduction">
  <h1>Introduction</h1>

  <p>
    <strong>AI Stack FastAPI</strong> is a production-ready boilerplate for building
    RAG (Retrieval-Augmented Generation) applications with complete vendor flexibility.
    It combines the speed of <strong>FastAPI</strong> with the modern frontend experience
    of <strong>Next.js 15</strong>.
  </p>

  <CardGrid cols={2}>
    <Card title="Zero Lock-in" icon="🔓">
      <p>
        Swap LLM or Vector DB with 1 environment variable. Abstract interfaces
        ensure portability.
      </p>
    </Card>
    <Card title="Production-First" icon="⚡">
      <p>
        Structured logging, async operations, cost tracking, and observability
        built-in.
      </p>
    </Card>
    <Card title="Type Safe" icon="🛡️">
      <p>
        Full TypeScript frontend with Pydantic models and SQLAlchemy 2.0 on the
        backend.
      </p>
    </Card>
    <Card title="Modern Stack" icon="🚀">
      <p>
        Next.js 15, FastAPI, Tailwind CSS, and your choice of LLM and vector
        database.
      </p>
    </Card>
  </CardGrid>

  <h2>Features</h2>

  <ul>
    <li>
      <strong>FastAPI Backend</strong>: Async/await, SQLAlchemy 2.0, Alembic
      migrations.
    </li>
    <li>
      <strong>Next.js 15 Frontend</strong>: App Router, Tailwind CSS,
      TypeScript.
    </li>
    <li>
      <strong>RAG Pipeline</strong>: Document chunking, embeddings, vector
      search, and reranking.
    </li>
    <li>
      <strong>4 LLM Providers</strong>: OpenAI, Anthropic, Gemini, Ollama.
    </li>
    <li><strong>3 Vector Databases</strong>: Qdrant, Weaviate, pgvector.</li>
    <li>
      <strong>Dual Auth</strong>: Clerk (primary) + JWT (service-to-service).
    </li>
  </ul>

  <h2>Quick Example</h2>

  <CodeBlock
    code={`# Create project
npx create-ai-stack@latest my-ai-app

# Start infrastructure
cd my-ai-app
docker-compose up -d

# Run migrations
cd apps/backend && uv run alembic upgrade head

# Start development
uv run uvicorn app.main:app --reload`}
    lang="bash"
  />

  <h2>Philosophy</h2>

  <p>
    We follow the <strong>AI Stack</strong> philosophy: opinionated but modular, typesafe,
    and production-ready from day one. You shouldn't have to rewrite your entire stack
    just to switch from OpenAI to Anthropic, or from Qdrant to pgvector.
  </p>

  <Callout type="tip">
    <p>Ready to build? <a href="/docs/quickstart">Get Started →</a></p>
  </Callout>
</DocsLayout>
